import { TodoListItemDirective } from './todo-list-item.directive';

describe('TodoListItemDirective', () => {
  it('should create an instance', () => {
    const directive = new TodoListItemDirective();
    expect(directive).toBeTruthy();
  });
});
